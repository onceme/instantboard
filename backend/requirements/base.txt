fastapi>=0.104.0
uvicorn[standard]>=0.24.0
sqlalchemy[asyncio]>=2.0.23
alembic>=1.13.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
email-validator>=2.0.0
httpx>=0.25.0
feedparser>=6.0.10
redis>=5.0.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
psutil>=5.9.0
apscheduler>=3.10.4
# Upper-bounded on purpose: an open-ended range let dev and staging drift to
# different asyncpg releases, which surfaced as behavior differences between
# environments (0.31 observed in the image vs. other versions locally). Keep
# the bound conservative; bump it deliberately after cross-environment testing.
asyncpg>=0.29.0,<0.32
beautifulsoup4>=4.12.0
lxml>=4.9.0
python-multipart>=0.0.6
# Pinned below 5: passlib 1.7.4 is incompatible with bcrypt>=5 (its backend self-check
# probes with a >72-byte password, which bcrypt 5 rejects with ValueError instead of
# truncating), breaking hash_password/verify_password at runtime.
bcrypt>=4.1.0,<5
yfinance>=0.2.31
